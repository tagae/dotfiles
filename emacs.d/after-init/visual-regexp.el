(global-set-key (kbd "C-c M-%") 'vr/query-replace)
(global-set-key (kbd "C-c C-M-%") 'vr/replace)

;; If using multiple-cursors:
;; (when (locate-library "multiple-cursors")
;;   (define-key global-map (kbd "C-c m") 'vr/mc-mark))
