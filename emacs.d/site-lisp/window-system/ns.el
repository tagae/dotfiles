;; Avoid tooltips.
(tooltip-mode 0)

;; Color theme.
(load-theme 'wombat)

;; Higher frames by default.
(setq default-frame-alist '((height . 60)))

;; Have keys behave as in X.
(setq mac-command-modifier 'meta)
(setq mac-option-modifier 'none)
;(setq ns-function-modifier 'hyper)
